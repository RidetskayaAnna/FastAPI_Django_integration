# 🛠️ FastAPI + Django интеграция

## 📋 Описание
Два взаимодействующих сервиса:
1. FastAPI сервис - предоставляет API для управления продуктами
2. Django сервис - получает данные из FastAPI и сохраняет в свою БД

## 🚀 Запуск проекта
```bash
# 1. Инструкции по запуску обоих сервисов (FastAPI и Django) с помощью Docker,
Инструкции по установке зависимостей (pip install -r requirements.txt)
docker compose up -d --build

# 2. Инструкции по запуску команды генерации тестовых данных (seed_data.py)
docker compose exec fastapi python -m app.seed_data

# 3. Инструкции по запуску management команды Django.
docker compose exec django python manage.py update_products_data
